name: Atualizar cobranças diárias

on:
  schedule:
    - cron: "0 12 * * *"  # 09:00 horário de Brasília (UTC-3)
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v3

      - name: ⚙️ Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: 📦 Instalar dependências
        run: pip install -r requirements.txt

      - name: 🚀 Rodar atualização de juros
        run: python main.py

      - name: ✅ Commit e push (atualização de clientes)
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data/clientes.json || true
          git commit -m "Atualização automática de cobranças diárias" || echo "Sem alterações a commitar"
          git push
